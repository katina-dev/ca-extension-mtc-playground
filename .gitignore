# MTC Bridge build artifacts
bin/
keys/
gen-certs

# Go
*.exe
*.test
*.out

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Generated
scripts/__pycache__/

# Docker
docker-compose.override.yml

# Secrets
*.key
.env
!config.example.yaml

# Generated TLS certs
acme-cert.pem
acme-key.pem

# Compiled binaries (stray output from `go build ./cmd/<name>/` without -o,
# which drops the artifact at the repo root). Leading / anchors each pattern
# so they don't also match the cmd/<name>/ source directories.
/demo-embedded-cert
/mtc-bridge
/mtc-conformance
/mtc-assertion
/mtc-interop
/mtc-tls-server
/mtc-tls-verify
/mtc-verify-cert
/cert-size-report
/bulk-issue

# Session exports (user-generated, not part of the project history)
/last-session-*.txt
/2*-*-*-*.txt
